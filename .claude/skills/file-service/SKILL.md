---
name: file-service
description: "The shared file-handling module. Use this for anything involving uploads, downloads, previews, signed URLs, MIME/content validation, size limits, filename sanitisation, versioning, MinIO/S3 configuration, or malware scanning across Sheets, Documents, and task attachments."
---

# file-service (pointer)

This is a pointer stub. The canonical skill lives at the repository root:

**Read and follow `skills/file-service/SKILL.md` now** (@skills/file-service/SKILL.md). Do not act on this stub alone — the full rules, code patterns, and spec references are in the canonical file. If the two ever disagree, the canonical file wins.
