---
name: data-model
description: "The Database app's data model. Use this whenever touching prisma/schema.prisma, writing a migration, adding or changing entity fields or enums, writing queries, or reasoning about relations, versioning, or the activity log."
---

# data-model (pointer)

This is a pointer stub. The canonical skill lives at the repository root:

**Read and follow `skills/data-model/SKILL.md` now** (@skills/data-model/SKILL.md). Do not act on this stub alone — the full rules, code patterns, and spec references are in the canonical file. If the two ever disagree, the canonical file wins.
